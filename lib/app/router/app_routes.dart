/// Centralised route path constants — mirrors the reference project's
/// `AppRoutes` pattern so paths are never hard-coded at call sites.
class AppRoutes {
  AppRoutes._();

  /// Product list — the app's home screen.
  static const products = '/';
}
